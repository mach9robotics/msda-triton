# yaml-language-server: $schema=https://coderabbit.ai/integrations/schema.v2.json
# Public repo: no auto-review; org members trigger via `@coderabbitai review`.
reviews:
  auto_review:
    enabled: false              # on-demand only
chat:
  allow_non_org_members: false  # org members only
